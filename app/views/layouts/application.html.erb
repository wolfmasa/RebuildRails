<!DOCTYPE html>
<html>
<head>
  <title>RebuildRails</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    <header class="navbar navbar-default" role="navigation">
        <div class="container" id="RR-logo">
            <div class="navbar-header">
                <%= link_to 'RebuildRails', root_path, class: 'navbar-brand' %>
            </div>
        </div>


        <div class="row" id="RR-search">
        <%= form_tag('/search', :method => 'get', :class => 'RR-serach-area') do %>

        <%= submit_tag 'Search', :class =>'btn btn-default RR-search-button'%>
          <%= search_field_tag('word', "", :class => 'RR-search-field') %>
        <% end %>
      </div>
    </header>

    <%= yield %>

</body>
</html>
